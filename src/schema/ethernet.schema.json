{
  "$schema": "http://json-schema.org/schema#",
  "title": "ethernet",
  "description": "Interface list",
  "type": "array",
  "items": {
    "type": "object",
    "title": "Interface entity",
    "required": ["name", "vid", "ip", "macaddr", "defaultroute", "proto"],
    "properties": {
      "name": {
        "type": "string",
        "description": "Interface name",
        "hidden": true
      },
      "vid": {
        "type": "number",
        "description": "VLAN ID",
        "default": 13
      },
      "switch": {
        "type": "string",
        "description": "Switch device"
      },
      "untagged":{
        "type": "array",
        "description": "Tagged ports"
      },
      "ip": {
        "type": "string",
        "description": "IP address config",
        "default": "dhcp"
      },
      "defaultroute": {
        "type": "boolean",
        "description": "Use as defaultroute",
        "default": false
      },
      "macaddr": {
        "type": "string",
        "description": "MAC address",
        "default": "de:ad:be:ef:12:34"
      },
      "mtu": {
        "type": "number",
        "description": "MTU"
      },
      "metric": {
        "type": "number",
        "description": "Network metric"
      },
      "proto": {
        "type": "string",
        "default": "vlan"
      }
   }
 },
   "commands": {
     "apply": {
       "command": "/home/art/Coding/zcore/src/zfilter2",
       "args": ["zfilter2", "ethernet.schema", "./apply.sh"],
       "json": "in"
    },
    "add": {
       "command": "/home/art/Coding/zcore/src/zgen",
       "args": ["zgen", "ethernet.schema", "./add.sh", "network", "interface"],
       "argpass": true,
       "cue": ["vid","switch"],
       "json": "out"
    }
  },
  "acquire": {
    "shell": "/home/art/Coding/zcore/src/zfilter",
    "args": ["zfilter", "ethernet.schema", "./ethernet.sh"]
  }
}
